"""Shared pytest fixtures.

Provides `test_db` (isolated `thought2do_test` database), `test_client`
(FastAPI TestClient), `auth_token` (pre-registered user JWT), and
`sample_tasks` (seeded task data) fixtures.
"""
